<Window x:Class="SaintSender.DesktopUI.Views.Details"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI.Views"
        mc:Ignorable="d"
        Title="Details" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Sender}" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{Binding Subject}" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="2" Text="{Binding Date}" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="3" Text="{Binding Message}" VerticalAlignment="Center"/>
        <Button x:Name="ReplyButton" Content="Reply" Width="50" Height="25" Click="ReplyButton_Click" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</Window>
